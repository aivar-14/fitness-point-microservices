spring.application.name=eureka-server
# Sets the port for the Eureka Server
server.port=8761

# These properties tell the server not to register itself as a client
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false